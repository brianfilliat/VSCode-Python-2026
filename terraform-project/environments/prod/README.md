# Prod Environment

Use this environment to discuss production controls, approval gates, and careful plan review.

```powershell
.\scripts\init.ps1 -Environment prod
.\scripts\validate.ps1 -Environment prod
.\scripts\plan.ps1 -Environment prod
```
