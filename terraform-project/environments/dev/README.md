# Dev Environment

Use this environment for first runs, experiments, and training exercises.

```powershell
.\scripts\init.ps1 -Environment dev
.\scripts\validate.ps1 -Environment dev
.\scripts\plan.ps1 -Environment dev
```
